#include<stdio.h>
int main()
{
	int i,j,k;
	while(i=1)
	{
		while(j=1)
		{
			if(k=1)
			{
				printf("saurav%d,%d,%d",i,j,k);
				break;
				
			}
			j--;
			break;
		//	printf("SAUARV%d,%d,%d",i,j,k);
		}break;
		i--;
	}
}